
# local state
.omc/
__pycache__/
*.pyc
.pytest_cache/

# diagram generation (tools/make-diagrams.mjs): fetched font + npm deps, never committed
# (the manifests too — `npm i roughjs opentype.js` writes them, and the deps are
# deliberately not vendored: the script runs by hand, not in CI)
.diagram-font/
node_modules/
package.json
package-lock.json
