---
description: Turn a raw idea into the head of a map (objective in the project's currency, current value, ceiling, gap, first levers) — draft, then start
---

Take my raw idea below and draft the head of the map for this project (skill handoff for the
shape, skill goal-setup for the method). Do NOT write a contract, phases or done-whens: the
deliverable is the first version of `HANDOFF.md` sections 1-3, printed for a 60-second read.

```
## 1. Objective and distance
<the objective in the project's currency: a number vs a physical ceiling / a user path that
works end to end / a question and the contribution so far> · now: <value or "unknown: first
lever is to measure it"> · ceiling or reference: <value, source> · gap: <x>

## 2. Levers
- <lever> — expected gain — the test that kills it in minutes   (from: map / head / outside: cite)
- ...  (include at least one combination and at least one from outside the repo)

## 3. Fog
- <known unknown>
```

Mark inferences with `[ASSUMED]`. Then list, in chat and phone format, only the decisions of the
four kinds (irreversible, spend, public, objective) with the default applied on a bare "ok".
If there are none, say so and start the first iteration.

Raw idea: $ARGUMENTS
